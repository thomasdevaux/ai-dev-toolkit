# Requirements

- REQ-001 — Provide a stub docs fixture for the `audit-docs` skill —
  rationale: exercises `process/light`'s `audit-docs` skill against
  real files instead of an empty directory — status: implemented
