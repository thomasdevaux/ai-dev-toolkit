#include <stdint.h>

/* Non-safety embedded C file in the combined automotive demo: exercises
 * stacks/embedded-c's misra rule only (outside src/safety/). */
int32_t read_fault_code(void) {
    return 0;
}
