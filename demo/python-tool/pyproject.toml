[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "demo-flash-tool"
version = "0.1.0"
description = "Dummy Python tool for validating the stacks/python block."
requires-python = ">=3.10"

[project.scripts]
demo-flash-tool = "demo_flash_tool.cli:main"
